# Example structure only. Copy outside the repository; every empty value is required.
# Register a confidential OIDC web client with exact HTTPS callback and no wildcard.
[auth]
redirect_uri = ""
cookie_secret = ""
client_id = ""
client_secret = ""
server_metadata_url = ""
# redirect_uri: https://your-cost-host/oauth2callback
# server_metadata_url: approved issuer's OpenID discovery URL
# Generate cookie_secret locally, e.g. python -c "import secrets; print(secrets.token_urlsafe(48))"
# Client secret is issued by the IdP administrator, never invented by this example.
