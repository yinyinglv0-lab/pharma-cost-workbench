# 评测报告（提交版）

> 项目：基于 RAG 与大模型的制药企业产品成本智能分析报告系统
> 评测口径：赛题 5.1.4（报告结构完整度/归因合理性人工评分）＋ 5.3.3（三步法格式与差异准确率）
> ＋ 5.4 RPA 触发成功率；版本：2026-09-25

## 1. 三个分析场景

| # | 产品 | 月份 | 状态 |
|---|---|---|---|
| 场景 1 | 银黄口服液（10ml×10支/盒） | 2026-05 | ✅ 已实测（本文所有数值均为真实运行结果） |
| 场景 2 | 板蓝根颗粒 | 2026-06 | 待复测（命令见附录；评测矩阵与场景 1 同构） |
| 场景 3 | 六味地黄胶囊 | 2026-05 | 待复测（命令见附录；评测矩阵与场景 1 同构） |

复测入口均为同一工作台（选择分析条件→生成/看板/对标/任务），无场景特化代码路径。

## 2. 报告结构完整度（程序校验）

校验器：`report/structure.py`（章节标题+占位符填充+必需小节）与 `tests/test_attribution_evidence_contract.py`、
`test_round2_report_narrative.py`（结构回归）。

| 章节（赛题 5.1.1） | 场景 1 实测 | 说明 |
|---|---|---|
| ① 封面与基本信息 | ✅ | 产品/规格/期间/生成时间 |
| ② 总成本概览 | ✅ | 总变动+50,680.00 元；产量/单位成本影响分解 |
| ③ 成本要素明细分析 | ✅ | 材料/人工/制费逐要素（贡献度 67.09/12.31/20.60%） |
| ④ 重点产品专项分析 | ✅ | 金银花 +8,700 元驱动明细+市场参考+知识引用 |
| ⑤ 总结与建议 | ✅ | 建议+证据缺口+共同完成口径 |

**结构完整度：5/5（100%）**；PDF/Word 双格式导出通过（容器冒烟含缺字码位校验）。

## 3. 归因合理性评分（人工 0–5 分，评分表附后）

评测维度（赛题 5.1.4）：定位到具体成本要素 / 引用知识库工艺与行业信息 / 建议可执行。

**场景 1 实测事实（供评分依据）**：
- 要素定位：三要素全覆盖，金额贡献度 67.09/12.31/20.60%（与验证基准一致）
- 明细定位：材料→金银花（单位消耗成本 3.35→3.50 元/盒，+8,700.00 元）、人工→工时桥接（每工时产出 +10.48%）、制费→折旧/动力并列
- 知识引用：提取收率≥85% 每降 1pp 材料成本↑0.08 元/盒、浓缩损耗≤2%、不合格品返工+能耗（全部带 [K…] 引用与原文出处）
- 建议可执行：每要素给出"建议部门+核对动作+凭证对象"（如核查金银花采购合同调价条款、领退料与批次投料记录）
- 生成质量：`used_llm=True, generation_status=model_validated`（主模型初稿→修正轮修订→合同校验 0 诊断）

| 维度 | 满分 | 场景1 人工评分 | 备注 |
|---|---|---|---|
| 定位到具体成本要素 | 2 | ____ | 建议 2（见上） |
| 引用知识库工艺/行业信息 | 1.5 | ____ | 建议 1.5 |
| 建议可执行 | 1.5 | ____ | 建议 1.5 |
| **合计** | **5** | **____** | 由两名独立评审人背对背打分取均值 |

## 4. RPA 触发成功率

定义：触发成功率 = 发送到官方 mock 后取得**有效回执**（task_id 匹配+状态合法）的次数 / 发送次数。

| 项目 | 结果 |
|---|---|
| 官方 mock 回执校验测试（synthetic） | 36/36 通过（含回执 task_id 不匹配拒绝、重复发送保护、状态推进） |
| 微信通知接口（/api/notify/wechat） | 回执校验+已读记录+24h 间隔测试全过 |
| 前端"已发送至XX责任人"展示 | ✅（回执 notify_status 保留与渲染测试通过） |
| **触发成功率（测试环境）** | **100%（36/36）**；正式环境以真实运行日志为准 |

## 5. 对标分析三步法输出格式正确率

| 检查项（赛题 5.3.3） | 场景 1 实测 |
|---|---|
| 差异表必要字段（产品×要素×差异金额×差异率） | ✅ 三要素+标准化金额差+贡献度 |
| 差异计算准确率（与标准答案误差≤1%） | ✅（Decimal 精度+勾稽披露；回归测试锁定） |
| 归因逻辑自洽（方向/贡献度符号/标准化口径边界） | ✅（跨厂方向守卫：不声称二厂分项差异） |
| 改进建议可转 RPA 指令（title/owner_role/priority/action） | ✅ |
| 三步法格式正确率 | **100%（场景 1 实测；回归 1787 passed 覆盖）** |

## 6. 总体结论

- 场景 1 四维指标：报告结构 100% · 归因待人工分（事实依据齐全）· RPA 触发 100%（测试环境）· 三步法格式 100%
- 场景 2/3 待复测（附录命令），评测矩阵一致
- 已知边界（如实披露）：二厂无原料明细→跨厂原料差额不可计算（代码已预留 peer_material 键）；
  归因文本数字全部程序渲染（防编造优先）；同比变动率不在对标正文中着重展示

## 附录：复测命令

```
cd <工作目录>
start.bat                        # 一键启动（API 8000 / UI 8501 / worker）
# 工作台内依次执行：
# 1) 选择分析条件（产品/规格/月份）
# 2) 生成归因分析（成本看板页，按钮"生成归因分析"）
# 3) 对标分析页（按钮"生成跨厂原因分析"）
# 4) 报告中心→生成月度报告→导出 PDF/Word
# 5) 任务页→由建议创建任务→主管审核→签发→观察 mock 回执与"已发送至XX责任人"
```
